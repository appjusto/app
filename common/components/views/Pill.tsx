import React from 'react';
import { View } from 'react-native';

import { colors } from '../../styles';

export default function () {
  return <View style={{ width: 4, height: 24, backgroundColor: colors.green, borderRadius: 8 }} />;
}
